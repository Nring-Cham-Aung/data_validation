import 'obj_type.dart';

/// * [Stack] represents a validation step with its type and associated value.
class Stack{
  final ObjType name;
  final dynamic value;
  const Stack(this.name, this.value);
}