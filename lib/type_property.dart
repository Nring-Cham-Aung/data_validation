

/// * [TypeProperty] defines properties for types, including nullability and default null value.
class TypeProperty{
  final bool nullAble;
  final dynamic nullValue;
  const TypeProperty(this.nullAble, this.nullValue);
}